name: mi_wallet
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  sqflite: ^2.4.2
  crypto: ^3.0.2
  shared_preferences: ^2.0.15
  intl: ^0.18.1
  fl_chart: ^0.64.0
  image_picker: ^1.0.4
  permission_handler: ^12.0.0+1   # ← Actualízalo a la última versión compatible

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# —————————————
# IMPORTANTE: aquí, al nivel raíz
dependency_overrides:
  permission_handler_android: ^12.0.0+1
# —————————————

flutter:
  uses-material-design: true
  assets:
    - assets/
